{
  "name": "electron-file-viewer",
  "version": "1.0.0",
  "description": "可扩展的文件查看器",
  "main": "src/main/index.js",
  "scripts": {
    "dev": "electron .",
    "build": "vite build",
    "start": "electron .",
    "pack": "electron-builder --dir",
    "dist": "electron-builder"
  },
  "dependencies": {
    "electron-updater": "^5.0.0"
  },
  "devDependencies": {
    "electron": "^25.0.0",
    "electron-builder": "^24.0.0",
    "vite": "^4.0.0",
    "vite-plugin-electron": "^0.11.0"
  },
  "build": {
    "appId": "com.example.file-viewer",
    "productName": "文件查看器",
    "directories": {
      "output": "dist"
    },
    "win": {
      "target": ["nsis"]
    }
  }
}
